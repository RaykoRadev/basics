function solve(input) {
    let n = Number(input[0]);

    for (let i = n; i < 0; i * 2) {
        console.log(i);


    }
}
solve([12,
    43.2144,
    12.3,
    543.23,
    -20,
]);
// solve([43.2144]);
// solve([12.3]);
// solve([543.23]);
// solve([-20]);
